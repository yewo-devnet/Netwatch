[flake8]
max-line-length = 99
extend-ignore = E203, W503
exclude =
    .git,
    __pycache__,
    .eggs,
    *.egg-info,
    build,
    dist

[tool:pytest]
testpaths = tests
addopts = --tb=short -q

[coverage:run]
source = netwatch
omit =
    tests/*
    setup.py
